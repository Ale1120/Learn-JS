var name = 'Alejandro',
surName = 'Ortegano'

var nameCl = name.toUpperCase()
var surNameTiny = surName.toLowerCase()

var firstLetterName = name.charAt(0)

var namequantity = name.length

var fullName = `${name} ${surName}`

var str = name.substr(0, 3)

var lastName = name.charAt(name.length - 1)

console.log(nameCl)
console.log(surNameTiny)
console.log(firstLetterName)
console.log(namequantity)
console.log(fullName)
console.log(str)
console.log(lastName)
